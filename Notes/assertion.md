
Assertion means the test is checking whether the actual result matches the expected result. 

Example - I clicked login. Now I should see the dashboard.

Actions perform something 
page.locator().click()
page.locator().fill()

Assertions verify something.
Without assertions, your script is only doing steps. 
With assertions, your script becomes a real test. 
