NPX does not have any official full form. It is just a tool name. 

What is the purpose of NPX? 
NPX helps us run local packages.

When we run this command, `npx playwright test`, `npx` first looks inside our project folder. It checks if Playwright is already installed. If it is there, it runs it.
 